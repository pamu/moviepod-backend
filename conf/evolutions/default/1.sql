# --- !Ups

create table "USER" ("ID" BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,"NAME" VARCHAR NOT NULL);
create table "MOVIE" ("ID" BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,"NAME" VARCHAR NOT NULL, "IMDB" VARCHAR NOT NULL, "USER_ID" BIGINT);

# --- !Downs

drop table "MOVIE";
drop table "USER";